spring:
  application:
    name: training-service
  profiles:
    default: local

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-components: always
  health:
    db:
      enabled: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true

logging:
  pattern:
    level: "%5p [traceId:%X{traceId:-} correlationId:%X{correlationId:-} userId:%X{userId:-} sessionId:%X{sessionId:-}]"

training:
  security:
    trusted-user-id-header: ${TRUSTED_USER_ID_HEADER:X-User-Id}
  openai:
    api-key: ${OPENAI_API_KEY:}
    timeout-ms: ${OPENAI_TIMEOUT_MS:30000}
    adapter: ${OPENAI_ADAPTER:local-fake}
    base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1/responses}
    model: ${OPENAI_MODEL:}
  outbox:
    publisher:
      enabled: ${OUTBOX_PUBLISHER_ENABLED:false}
      batch-size: ${OUTBOX_PUBLISHER_BATCH_SIZE:50}
      fixed-delay-ms: ${OUTBOX_PUBLISHER_FIXED_DELAY_MS:5000}
      retry-backoff-base-seconds: ${OUTBOX_RETRY_BACKOFF_BASE_SECONDS:60}
      retry-backoff-max-seconds: ${OUTBOX_RETRY_BACKOFF_MAX_SECONDS:3600}
  event-broker:
    url: ${EVENT_BROKER_URL:}
    timeout-ms: ${EVENT_BROKER_TIMEOUT_MS:5000}
